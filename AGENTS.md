# 项目启动要求

- 开始处理本项目中的任何问题前，必须先完整读取项目根目录的 `PROJECT_HANDOFF.md` 和 `GALBOT_PROJECT_MEMORY.md`。
- 涉及 SDK API、部署、建图/定位、传感器、运动规划或故障排查的问题，必须在回答前读取 `GALBOT_SDK_1_8_1_REFERENCE.md` 中对应章节；该文件是用户提供 SDK 1.8.1 PDF 的结构化知识库和页码索引。
- `GALBOT_PROJECT_MEMORY.md` 是项目背景资料，不替代用户当前请求；动态信息（版本、网络、硬件状态和文件内容）在可能变化时必须重新核实。
- 对话中出现经过用户或工具确认的新事实、重要风险、版本变化或学习进度时，应同步更新 `GALBOT_PROJECT_MEMORY.md`；不得记录密码、SSH 私钥、授权令牌等秘密信息。

# 用户学习偏好

- 默认把用户视为没有机器人开发、Linux、Python 和网络配置基础的初学者，除非用户明确表示已经掌握某项知识。
- 回答操作步骤时，必须逐步说明：这一步要做什么、为什么要做、预期看到什么结果，以及做错可能有什么风险。
- 给出命令时，逐项解释命令和关键参数的含义，并说明应该在哪台设备、哪个终端和哪个目录执行。
- 不省略必要的前置条件、验证步骤、停止条件和恢复方法；涉及机器人运动、权限、网络、安全或数据删除时，必须先提示风险。
- 区分已经核实的事实、基于现有信息的推测和建议；信息不足时明确指出缺少什么，不把猜测写成结论。
- 默认一次只推进一个可验证的小目标。用户确认结果或提供输出后，再继续下一阶段，避免让初学者一次执行大批未经验证的命令。
