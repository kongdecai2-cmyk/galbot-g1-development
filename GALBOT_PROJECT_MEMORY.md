# 银河通用机器人 G1 项目知识库

最后更新：2026-09-10

## 文件用途

这是本项目的持续背景资料。新对话开始工作前必须先完整读取本文件。

完整对外交接材料见 `PROJECT_HANDOFF.md`。交接给新的协作者或新的 GPT 时，应同时提供该文件、`AGENTS.md` 和本文件。

SDK 1.8.1 的结构化知识库见 `GALBOT_SDK_1_8_1_REFERENCE.md`。后续涉及 SDK API、部署、建图/定位、传感器、运动规划或故障排查的问题，应先读取该文件的相关章节和页码引用；它是用户提供 PDF 的索引与安全解读，不替代真机和已安装 SDK 的实时核验。

2026-09-10 已在桌面建立交接包 `C:\Users\ASUS\Desktop\银河机器人开发_交接_2026-09-10`。该交接包应包含四份 Markdown、SDK 1.8.1 原始 PDF、旋转脚本、七份训练示例和 SHA-256 完整性清单；交付前必须核对清单。

2026-09-10：用户确认建立团队协作用的私有 GitHub 仓库，目标为个人账号 `kongdecai2-cmyk` 下的 `galbot-g1-development`。范围包括当前工作目录文件、交接包中的 SDK PDF、七个训练脚本、重建后的 SHA-256 清单，以及已查阅官方链接的资料索引。用户确认团队有权在该私有仓库内共享 PDF 和训练脚本，并要求保留真实机器人内网 IP；Git 提交身份使用 `kongdecai2-cmyk <kongdecai2@gmail.com>`。首次上传阶段不邀请团队成员。

GitHub 仓库用于传递结构化项目上下文，不能复制完整对话状态或替代真机实时核验。新的协作者或 GPT 仍必须按 `README.md`、`PROJECT_HANDOFF.md`、`AGENTS.md` 和本文件的顺序完成项目接管。

使用原则：

- 将“已核实事实”“合理推测”和“建议”分开。
- 机器人版本、网络地址、硬件配置和文件内容可能变化，执行操作前需要重新验证。
- 本文件不记录密码、SSH 私钥、授权令牌或厂商公开的默认密码。

## 用户目标与教学方式

- 用户是银河通用机器人二次开发初学者，目标是学会 Galbot G1 的 Python 二次开发和真机操作。
- 用户已经获得开发授权。
- 所有操作说明需要逐步解释：做什么、为什么、预期结果和风险。
- 每次新增、修改或交付由助手编写的脚本时，必须面向零基础用户解释其用途、整体执行顺序、关键代码块和变量、函数输入输出、单位、异常与清理逻辑、运行前提、验证方法及已知局限；使用实际文件和行号，不只给出运行命令。
- 默认一次只推进一个可以验证的小目标，用户提供结果后再进入下一阶段。
- 不迎合用户；发现错误前提、代码缺陷或安全风险时直接指出并给出依据。

## 已核实的真机与开发环境

以下信息由用户截图确认：

- Windows 电脑使用 VS Code Remote SSH 连接机器人。
- 电脑和机器人连接同一个路由器 Wi-Fi。
- SSH 已连接成功，目标地址为局域网地址 `192.168.102.49`。
- SSH 用户为 `galbot`，机器人主机名为 `galbot-echo`。
- 用户已在机器人上创建个人脚本目录 `/userdata/KDC`，当前 SSH 用户 `galbot` 对该目录有写入权限。后续用户自编脚本默认保存于此目录。
- `galbot_sdk check-version` 输出：
  - SDK 版本：`1.8.1`
  - SDK 要求的 GBS：`GBS_1.16.x`
  - 机器人当前 GBS：`GBS_1.16.0.2.rc88`
  - 检查结果：`MATCH`
- 当前组合兼容，应继续使用 SDK 1.8.1，不应单独升级 SDK 1.9.1。
- VS Code 截图显示远程 Python 为 3.8.10，但正式开发前仍可用 `python3 --version` 和解释器路径再次核实。

仍未核实：

- G1 的准确硬件小版本。
- 左右末端执行器是夹爪、吸盘还是其他配置。
- 机器人当前地图、定位和导航配置。
- 用户是否完成实体急停测试及真机安全培训。

## 文档调研结论

用户提供的文档：

`E:\WXWork\1688856602736104\Cache\File\2026-09\Galbot_SDK_G1_1.8.1SDK、API开发文档.pdf`

已核实：

- PDF 共 589 页，约 12.35 MB。
- 内容来源指向官方 SDK 1.8.1 网页文档，但 PDF 来自微信缓存、带第三方水印，应视为网页快照，不是厂商签名原件。
- 文档包含部署、网络、建图定位、10 个教程、Python/C++ 示例、API 参考、故障排除和许可。
- SDK 1.8.1 对应 GBS 1.16.x，与当前机器人匹配。

文档与现状存在的差异或问题：

- 当前公开 SDK 已有 1.9.1，但当前机器人不能因此直接升级。
- PDF 的 Docker 章节标为 1.8.1，却示例使用 1.8.0 镜像。
- PDF 示例路径与当前仓库目录结构可能不同。
- 文档部分操作建议关闭全部 Windows 防火墙、使用递归 `chmod 777` 或手工修改底层网络配置，不适合作为新手默认方案。
- 部分示例关闭碰撞检测或使用高频直通控制，不应作为第一次真机程序。
- PDF 许可页称软件为机密专有软件，但官方 GitHub 的 V1.8.1 标签包含 Apache-2.0 许可证。代码许可应以对应官方标签内的 LICENSE 为准；固件、模型和商业服务仍可能受单独协议约束。
- 2026-09-10：已将 PDF 内容按来源、接口、单位、坐标系、教程、部署、导航、故障排查和许可差异整理进 `GALBOT_SDK_1_8_1_REFERENCE.md`；该文件标注了不能作为新手默认操作的高风险文档内容。
- 2026-09-10：用户要求把原始 PDF 也纳入交接包；PDF 副本与 SHA-256 校验值必须一并交付，接收方应先读结构化知识库再查原文上下文。

## 官方资料入口

- 官方 GitHub 组织：https://github.com/GalaxyGeneralRobotics
- GalbotSDK：https://github.com/GalaxyGeneralRobotics/GalbotSDK
- SDK 1.8.1 标签：https://github.com/GalaxyGeneralRobotics/GalbotSDK/releases/tag/V1.8.1
- SDK 1.9.1 在线文档：https://developer.galbot.com/docs/SDK/1.9.1/g1/zh/
- G1 使用手册：https://developer.galbot.com/docs/g1/2.2.4/zh/g1
- 开发者平台：https://developer.galbot.com/platform/
- 官方训练营：https://developer.galbot.com/training/
- G1 ROS 2/URDF/MJCF/USD 模型：https://github.com/GalaxyGeneralRobotics/galbot_one_golf_description
- MCAP 转 LeRobot 工具：https://github.com/GalaxyGeneralRobotics/galbot-mcap2lerobot

## SDK 心智模型

主要模块：

- `GalbotRobot`：机器人初始化、关节、底盘、夹爪/吸盘、传感器、音频和系统状态。
- `GalbotMotion`：FK/IK、运动规划、碰撞检测、障碍物和工具配置。
- `GalbotNavigation`：定位、路径可达性、导航、停止和状态查询。
- `GalbotPerception`：感知模型初始化、推理和结果读取。

文档中的 G1 2.2 关节组：

- `head`：2 个关节。
- `leg`：5 个关节。
- `left_arm`：7 个关节。
- `right_arm`：7 个关节。
- 左右夹爪、左右吸盘分别独立控制。

导航前置流程：建图、地图检查、电子围栏、定位、路径可达性检查、执行导航。文档建议定位评分大于 0.75 后再导航。

## 真机安全基线

- 无线 SSH 可以用于编辑和普通调用，但断线不等于机器人程序停止；实体急停是异常运动时的首要停止手段。
- 第一次运行运动程序前，清空机器人和双臂运动范围，安排人员守在急停旁。
- 不把 `Ctrl+C`、关闭 VS Code 或断开 Wi-Fi 当作可靠急停。
- 不默认关闭系统防火墙，不随意修改 HPU/XCU 地址，不使用来源不明的默认凭据。
- 初学阶段优先使用低速、阻塞、带超时的 `set_joint_positions`；不要先使用 `set_joint_commands`。
- “程序返回成功”只表示接口报告成功，不代表轨迹在现实环境中一定无碰撞。
- `request_shutdown()` 是关闭 SDK 资源，不等于把机器人移动到安全姿态。
- 故障排除中的解除限位、底层电机操作、控制器配置修改和底层服务重启，应交给厂商支持或有经验人员。

## Windows 到机器人文件传输规范

- 用户要求所有从 Windows 电脑传到机器人的脚本和配置文件默认使用可验证流程：本机执行 `Get-FileHash -Algorithm SHA256`，使用 `scp -p` 上传，机器人端执行 `sha256sum`，确认两端哈希完全一致后才视为传输成功。
- 上传 Python 脚本后，还应在机器人端执行 `python3 -m py_compile <文件名>`；这只验证语法，不会执行或控制机器人。
- 不把 VS Code 拖拽上传作为需要严谨确认的文件的最终验证方法；拖拽可以使用，但仍必须完成双端 SHA-256 校验。
- 默认机器人个人脚本目录为 `/userdata/KDC`，除非用户明确指定其他位置。

## 七个训练脚本的实际作用

Windows 原始文件位于 `C:\Users\ASUS\Desktop\银河\`；机器人 VS Code 中可见一份位于 `userdata/basic_day1` 的副本。运行前必须确认两边内容是否一致。

### 01_example_sdk_check.py

- 初始化 `GalbotRobot`，读取激活关节名称和数量，然后清理资源。
- 从代码上看没有下发运动指令。
- 风险较低，是当前最适合先运行的脚本。

### 02_example_joint_control.py

- 读取右臂关节位置、速度、加速度、力矩和电流。
- 使用 `set_joint_positions` 将右臂 7 个关节移动到硬编码角度。
- 速度为 0.1 rad/s、阻塞模式、超时 30 秒。
- 注释声称包含左右臂 14 个关节，但实际只有右臂 7 个值，注释有误。
- 没有显式运动规划或障碍物碰撞检查，不应直接当作安全动作。

### 03_example_heart.py

- 记录左右臂原姿态，同时移动双臂形成比心动作，再恢复原姿态。
- 速度为 0.1 rad/s，失败会重试。
- 提示文字称展示 15 秒，代码实际只等待 5 秒。
- 双臂运动范围较大，风险高于单臂测试。

### 04_example_teach_pick_place_pose.py

- 打开夹爪并将右臂移动到固定起始姿态。
- 操作人员按住末端绿色示教按钮，拖动机械臂并记录起始、预抓取、抓取、预放置和放置关节角。
- 测试夹爪闭合/张开，并把数据保存为 JSON。
- 默认输出相对路径为 `day1/code/teach_grasp_right_arm.json`，实际位置取决于程序运行目录。
- 夹爪宽度和 effort 为硬编码值，需要根据实际末端和物体核实。

### 05_example_pick_place_fixed.py

- 读取 04 生成的 JSON，依次回放固定位置抓取和放置。
- 不使用视觉，不会根据物体实际位置自动修正。
- 机器人、桌面、物体和底座相对位置改变后，原轨迹可能失效并发生碰撞。
- 回放闭合宽度比示教值小 0.003 m，夹持更紧但风险也增加。

### 06_example_packing.py

- 启动全部关节控制器，依次移动头部、左臂、右臂、腿部到装箱姿态。
- 这是运输/维护用途的全身姿态工具，不是普通基础练习。
- 某一动作失败后代码可能继续后续动作，且失败会自动重试；这种策略存在安全风险。

### 07_example_whole_body_reset_zero_odom.py

- 定义了 21 个关节目标和底盘目标，但这些变量实际没有被使用。
- 真正调用的是 `zero_whole_body_and_base(...)`，可能驱动全身关节和底盘回零。
- `GalbotMotion` 被初始化但未使用。
- 清理代码没有放在 `try/finally` 中，异常时资源清理不可靠。

## 推荐学习顺序与当前进度

推荐顺序：

1. 运行 01，只验证初始化和只读接口。
2. 将 02 临时改成只读取右臂，理解关节名、弧度和状态。
3. 比较当前角度与小幅目标角度，完成第一次低速单关节/单臂运动。
4. 再学习双臂动作和人工示教。
5. 完成安全示教后才回放固定抓放。
6. 最后学习运动规划、碰撞检测、建图导航和感知。

06 和 07 不属于推荐学习主线，只在明确理解其用途并满足真机安全条件时使用。

当前下一步：先在机器人远程终端运行 `01_example_sdk_check.py`，保留完整输出；不要按文件编号连续运行 02-07。

## 新增底盘旋转脚本

- 项目文件：`king_example_base_rotate_clockwise_360.py`。
- 依据 SDK 1.8.1 文档使用 `set_base_velocity(linear_velocity, angular_velocity, duration_s)`，并用 `stop_base()` 主动停止。
- 脚本采用 0.1 rad/s 的低角速度，先执行 15 度方向测试；用户确认从上方观察为顺时针后，再执行剩余 345 度。
- 使用负 z 角速度表示顺时针，依据常见 `base_link` 右手坐标约定；现场方向测试是必要验证，不能只依赖符号假设。
- 该方法通过“角速度乘时间”得到名义 360 度，是开环控制。轮胎打滑、加减速和地面条件会造成角度误差，不能描述为精确 360 度。
- 无线 SSH 断开时机器人端程序仍可能继续，脚本的定时自动停止和 `finally` 停止只是软件保护，不能替代实体急停。
- 2026-09-10：脚本已通过 Windows 与机器人端的 SHA-256 双端校验（`D60E04ED777818E3945B8D56486152C44EA0A7936ABAAED3D14D12359BF4C92C`），并已在机器人 `/userdata/KDC` 中通过 `python3 -m py_compile` 语法检查；尚未运行，未向机器人下发底盘运动命令。
- 2026-09-10：用户已在真机运行该脚本。终端日志显示初始化成功、15 度方向测试完成、用户确认方向后完成剩余 345 度、`stop_base()` 两次均报告成功，并正常释放 SDK 资源。该记录证明软件命令链路正常完成；由于脚本是开环定时控制，尚未独立量测真实物理旋转角度是否精确为 360 度。

## 新增“前进、左转、比心”串联脚本

- 2026-09-10：在本项目创建 `galbot_forward_left_forward_heart.py`。它以 0.10 m/s 的定时速度分别前进约 10 秒、左转约 15.71 秒、再前进约 10 秒，然后通过独立 Python 进程调用 `/userdata/basic_day1/03_example_heart.py`。
- 这不是闭环里程计或定位控制：两段“1 米”和“90 度”均为名义值，会受轮胎打滑、加减速和地面条件影响。运行前应在有急停观察员的空旷区域完成小范围方向和距离校验。
- 比心脚本原件当前不在本项目工作区；其机器人路径来自此前记录，尚未在本次任务中重新验证。串联脚本会在实际调用前检查该文件存在。
- 该脚本使用正 `wz` 作为从上方看左转，这是常见 `base_link` 右手坐标约定，但尚未在当前机器人上单独验证。脚本要求操作人员在启动前确认该方向。
- 在启动比心子脚本前，串联脚本会停止底盘并释放其自身的 `GalbotRobot` SDK 资源，避免与比心脚本自身的 SDK 初始化发生控制资源冲突。仅完成本机 `python -m py_compile` 语法检查；尚未传输或在机器人上运行。
- 2026-09-10：用户决定自行手动上传 `galbot_forward_left_forward_heart.py` 到机器人。此决定不表示上传已完成；机器人端路径、文件 SHA-256 和 `python3 -m py_compile` 结果仍待上传后核实。
- 2026-09-10：用户在 Windows PowerShell 已计算本机源文件 SHA-256，截图确认结果为 `DE5BE2C4EA6556D49F704B52A580E4B0C49CE39A8B7FF866CBAFD629DF873B23`，与创建后记录的校验值一致。上传和机器人端校验尚未完成。
- 2026-09-10：用户手动通过 `scp -p` 上传脚本到 `galbot@192.168.102.49:/userdata/KDC/`。第一次连接 TCP 22 端口超时并关闭；第二次在输入密码后成功传输，终端显示 `100% 6584` 并返回本机 PowerShell 提示符。机器人端 SHA-256 和 `python3 -m py_compile` 尚未执行，不能据此断言机器人副本完整或可运行。
- 2026-09-10：用户在机器人终端完成上传后验证。`sha256sum /userdata/KDC/galbot_forward_left_forward_heart.py` 输出 `de5be2c4ea6556d49f704b52a580e4b0c49ce39a8b7ff866cbafd629df873b23`，与 Windows 源文件一致；`python3 -m py_compile /userdata/KDC/galbot_forward_left_forward_heart.py` 无输出并返回提示符，语法检查通过。脚本尚未运行；以上验证不证明底盘行进距离/转向方向准确，也不证明比心动作可安全执行。
- 2026-09-10：运行前在机器人端重新核实比心脚本路径：`ls -l /userdata/basic_day1/03_example_heart.py` 显示常规文件存在，属主/组为 `galbot`，大小 6205 字节，时间戳为 7 月 26 日 12:23。其内容及现场动作安全性未在本次运行前重新审查。
- 2026-09-10：运行前重新执行 `galbot_sdk check-version`。终端显示 SDK `1.8.1`，所需 GBS `GBS_1.16.x`，机器人当前 GBS `GBS_1.16.0.2.rc88`，检查结果 `MATCH`。SDK/GBS 兼容已在本次运行前核实；这不验证底盘方向、实际行进精度、环境障碍物或双臂动作安全性。
- 2026-09-10：用户在启动串联动作前明确确认：L 形路线和终点区域已清空、比心双臂扫掠范围无障碍物、机器人未连接充电线、机器人姿态/电量/底盘状态正常，且有人员全程守在已测试可用的实体急停按钮旁。这是操作人员现场确认，不是软件自动检测结果。
- 2026-09-10：用户在真机成功运行 `/userdata/KDC/galbot_forward_left_forward_heart.py`。用户输入 `RUN_SEQUENCE` 后，日志依次显示第 1 段名义前进 1 米（10.00 秒）、第 2 段名义左转 90 度（15.71 秒）、第 3 段名义前进 1 米（10.00 秒），每段后 `stop_base()` 均报告成功。用户随后输入 `HEART`；外层 SDK 资源成功释放，比心子脚本在用户确认后初始化成功、下发双臂比心关节角成功、恢复原始双臂关节角成功并释放资源，外层脚本打印“整个动作序列完成”。
- 上述真机日志证明软件命令链路完成，不能据此断言实际位移恰为 1 米、实际转角恰为 90 度、持续时间恰为提示文字中的 15 秒或现场全程无碰撞。当前比心脚本输出“Showing heart gesture for 15 seconds”，但此前归档代码记录其实际等待代码为 5 秒；需要在后续阅读当前机器人脚本内容后才能判断这是文案差异还是文件版本差异。
